---
name: vtiger-bs5-migration
description: Reference for the vtiger CRM 8.4 Bootstrap 3→5 / jQuery 2→3 modernization project. Use whenever working on this vtiger codebase's UI, CSS, JS plugin, or Bootstrap-related code — class/attribute renames, jQuery-plugin-to-vanilla-JS conversions, plugin tier decisions, or debugging "looks broken after a change" issues. Also use when the user asks what's been done, what's deferred, or why a past decision was made on this project.
---

# Vtiger BS3→BS5 Migration Reference

Full log of the modernization work on this vtiger 8.4 install: what changed, why, and what's still pending. Read this before touching Bootstrap/jQuery-related code in this repo so you don't redo work or reintroduce a fixed bug.

**⚠️ Trust git history over this log, always.** On 2026-09-22 this log claimed the class-rename sweep and JS API conversion were both complete app-wide. They weren't — git history had only 4 commits total, touching a narrow slice of files (mostly `Vtiger/` core + `helper.js`/`application.js`/`app.js`), while live grep found ~230 template/JS files across nearly every module still on raw Bootstrap 3 markup, including `helper.js`/`Tag.js`/`Utils.js` themselves — the exact files this log said were "most central, already converted." The work described below as done was re-verified against the actual repo state on 2026-09-22 and is now accurate as of that date. If you're reading this later and something looks unswept, believe the grep, not the log — run `git log --oneline -- <file>` and a fresh grep before assuming a past session's claims here are still true.

## Class/attribute renames — swept app-wide across `layouts/v7/modules` + `layouts/v7/resources` (verified 2026-09-22), don't redo

| Old (BS3) | New (BS5) | Notes |
|---|---|---|
| `data-toggle="..."` | `data-bs-toggle="..."` | |
| `data-target="..."` | `data-bs-target="..."` | **Not a blind rename** — `data-target` is also used as a plain custom data attribute in 4 files unrelated to Bootstrap (`PurchaseOrder/uitypes/Text.tpl`, `Contacts/uitypes/Text.tpl`, `Accounts/uitypes/Text.tpl`, `Inventory/partials/EditViewContents.tpl` — all `copyAddress`/address-type selectors read via `.data('target')` in the matching `Edit.js` files). Those were deliberately left as bare `data-target=`. Before renaming any `data-target`, check it's actually paired with `data-toggle="collapse"`/`"modal"` on the same element, not a custom attribute. |
| `data-dismiss="..."` | `data-bs-dismiss="..."` | Missed in the original pass — 68 occurrences found later, all real (modal/alert close buttons) |
| `data-parent="..."` | `data-bs-parent="..."` | Accordion/collapse parent — 5 occurrences, all real |
| `data-ride="..."` | `data-bs-ride="..."` | Carousel autoplay |
| `data-slide="..."` / `data-slide-to="..."` | `data-bs-slide="..."` / `data-bs-slide-to="..."` | Carousel controls — only in `Settings/ExtensionStore/Detail.tpl` |
| `navbar-fixed-top` | `fixed-top` | |
| `hidden-xs`, `hidden-sm`, etc. | `d-none d-md-block` (adjust breakpoint) | BS4+ dropped `hidden-*`/`visible-*`. `hidden-xs hidden-sm` (either word order) → `d-none d-md-block`; a lone `hidden-xs` (3 spots) → `d-none d-sm-block` |
| `pull-left` / `pull-right` | `float-start` / `float-end` | |
| `navbar-toggle` | `navbar-toggler` | Needed for BS5's auto-hide-at-desktop behavior. **Gotcha:** a naive `sed 's/navbar-toggle"/navbar-toggler"/'` only catches it as the *last* class before the closing quote — 2 files had `class="navbar-toggle collapsed ..."` with trailing classes and were missed until a follow-up grep for `navbar-toggle[^r]` caught them |
| `.navbar-right` | `margin-left: auto` (custom CSS) | BS5 has no equivalent class — **no markup changes needed**, just add `.navbar-right { margin-left: auto; }` to `nav.css`; the class stays in the markup as a pure CSS hook |

**JS-side attribute reads must be updated in lockstep with the markup rename**, or they silently break: `.data('toggle')` (jQuery camelCases `data-bs-toggle` to `bsToggle`, not `toggle`) and `[data-toggle]` selectors stop matching once markup says `data-bs-toggle`. Found and fixed in `Vtiger/ListSidebar.js`, `Vtiger/AdvanceSearchList.js`, `Vtiger/SearchList.js`, `Vtiger/List.js`, `Reports/List.js`, `Reports/ChartDetail.js`.

If you find an un-swept instance, it's a real bug. Do a full-tree grep before assuming it's already handled — don't trust a prior sweep's claimed file count.

## Critical structural fixes (not obvious, easy to miss)

**`navbar-expand-lg` is required on `<nav class="app-fixed-navbar">`.** Without any `.navbar-expand-*` class, BS5 keeps `.navbar-collapse` content (search box, quick-create, user menu — basically the whole right side of the top bar) hidden at ALL widths. BS3 auto-showed this above a breakpoint via CSS baked into `.navbar-collapse` itself; BS5 requires explicit opt-in via `.navbar-expand-{breakpoint}`.

**Side effect:** `navbar-expand-lg` also sets `flex-wrap: nowrap` on the nav. This vtiger markup stacks TWO separate `.container-fluid` blocks inside one `<nav>` (top user bar + breadcrumb bar below it) — `flex-wrap:nowrap` forces them onto one line, splitting the nav's width ~50/50 between them instead of stacking. Fix (applied in `layouts/v7/lib/modern/css/nav.css`):
```css
.app-fixed-navbar.navbar-expand-lg { flex-wrap: wrap !important; }
```

**`.navbar-right` and `float-end` on flex children:** `.nav` is `display:flex` in BS5, and floats have no effect on flex items. `.navbar-right` (BS3 float helper) also doesn't exist in BS5. Fix (applied in `nav.css`, no markup changes): `.navbar-right { margin-left: auto; }` — covers all 12 occurrences (`ModuleHeader.tpl`/`CalendarHeader.tpl`/`ListViewHeader.tpl`/`Topbar.tpl` across modules).

**`.caret` markup duplicates BS5's own arrow.** BS5's `.dropdown-toggle` auto-draws an arrow via CSS `::after`. Old markup still has literal `<span class="caret">`/`<i class="caret">` in 31 files — don't try to restyle it, just hide it (applied in `nav.css`): `.caret { display: none; }`.

**Popper is required for dropdowns/tooltips/popovers to position correctly.** Load `bootstrap.bundle.js` (includes Popper) or load Popper separately before plain `bootstrap.js`.

## jQuery 3 regressions found and fixed

- `.size()` → `.length` (`.size()` removed in jQuery 3)
- `.live()` → `.on()` (delegated event binding; `.live()` fully removed)
- `$.browser` fully removed. Real code (`PrintReport.tpl`) and vendor code (jqplot) both depended on it. Fix: a global shim right after jQuery loads — `jQuery.browser = jQuery.browser || {};` — stops crashes without restoring real detection (fine, since no modern browser is IE anyway).
- `jQuery(window).load(fn)` is ambiguous with jQuery's AJAX `.load()` method in jQuery 3 and doesn't reliably bind as a window-load event. Fix: `jQuery(window).on('load', fn)`.
- jstorage plugin deletion broke internal wrapper functions in `application.js` (`storage.get/set/delete/flush`) and root `resources/app.js` (`cacheGet/cacheSet/cacheClear`) — both rewritten to call native `localStorage` directly. **Lesson: check for internal wrapper dependents before deleting any plugin, not just external template/module usage.**

## BS5 JS API — jQuery-plugin calls converted to vanilla JS classes

BS5 removed the jQuery-plugin style entirely (`$(el).modal('show')` etc no longer works out of the box). **Explicit project decision: no compatibility shim — direct call-site rewrites in every file.** A shim (monkey-patching `jQuery.fn.modal` etc to delegate to BS5 classes) was proposed and rejected.

Conversion patterns used throughout:

| Old call | New call |
|---|---|
| `X.modal(options)` | `bootstrap.Modal.getOrCreateInstance(X[0], options).show();` |
| `X.modal('hide')` | `var _m = bootstrap.Modal.getInstance(X[0]); if(_m) _m.hide();` |
| `X.tooltip(options)` | `bootstrap.Tooltip.getOrCreateInstance(X[0], options);` |
| `X.popover(options)` | `bootstrap.Popover.getOrCreateInstance(X[0], options);` |
| `X.popover('show'/'hide'/'toggle')` | `var _p = bootstrap.Popover.getInstance(X[0]); if(_p) _p.show()/.hide()/.toggle();` |
| `X.popover('destroy')` | `var _p = bootstrap.Popover.getInstance(X[0]); if(_p) _p.dispose();` |
| `X.dropdown('toggle')` | `bootstrap.Dropdown.getOrCreateInstance(X[0]).toggle();` |
| `X.collapse('show'/'hide')` | `bootstrap.Collapse.getOrCreateInstance(X[0]).show();` or via `getInstance().hide()` |

All 20 affected app-code files (vendor/lib excluded from scope) have been converted, verified 2026-09-22: `helper.js` (most central — 10 modal calls), `Emails/MassEdit.js`, `Emails/EmailPreview.js`, `Settings/Workflows/Edit.js`, `Vtiger/Utils.js`, `Settings/CustomerPortal/CustomerPortal.js`, `Vtiger/ListSidebar.js`, `Vtiger/Detail.js`, `Vtiger/List.js`, `Vtiger/Tag.js`, `Calendar/TaskManagement.js`, `MailManager/List.js`, `Settings/SMSNotifier/List.js`, `Settings/ExtensionStore/ExtensionStore.js`, `Products/Detail.js`, `Inventory/Detail.js`, `Inventory/Edit.js` (25 calls, same handful of patterns repeated — 4 more are inside a comment, `// chargesTrigger.popover('hide');` etc, deliberately left alone), `Leads/Detail.js`, `Potentials/Detail.js`. `Vtiger/validation.js` had a `.popover()` block that turned out to be dead/commented code — skipped, don't "fix" commented-out code.

Extra nuances hit during conversion, worth knowing before doing more of these:
- **Multi-element selectors need `.each()`**, not `X[0]`: class/attribute selectors like `jQuery('[rel="tooltip"]')` or `jQuery('.totalCostCalculationInfo')` can match more than one node — `getOrCreateInstance` takes exactly one element. Single-element cases (an ID selector, or a variable already scoped to one node via a click handler's `e.currentTarget`) are safe to use `X[0]` directly.
- **`.data('bs.popover').tip()` has no BS5 equivalent.** Old code used this to grab the generated tip element synchronously right after init. BS5 doesn't expose `.tip()`; instead, hook `shown.bs.popover` and read `element.attr('aria-describedby')` to find the actual tip element in the DOM (`Products/Detail.js`).
- **`.removeData("modal").modal(newParams)` (force re-init with fresh options)** becomes: `getInstance(...)?.dispose()` first, then `getOrCreateInstance(el, newParams).show()` — `getOrCreateInstance` reuses an existing instance's original config if one exists, so you must dispose before re-creating with different params (`helper.js` `showModal`/`hideModal`, `Emails/MassEdit.js`).
- **The old sanitizer whitelist API renamed**: `X.popover.Constructor.DEFAULTS.whiteList` (BS3/4, per-instance) → `bootstrap.Popover.Default.allowList` (BS5, static on the class, and renamed `whiteList`→`allowList`) (`MailManager/List.js`).
- Guard `getOrCreateInstance`/`getInstance` calls with a null/existence check wherever the original jQuery selector could legitimately match nothing (e.g. `#phoneFormatWarningPop` only exists on modules with phone fields) — jQuery silently no-ops on an empty selection, but `bootstrap.Popover.getOrCreateInstance(undefined)` throws.

If you find more `.modal(`/`.tooltip(`/`.popover(`/`.dropdown(`/`.collapse(` calls in app code (not vendor `/lib/` folders), convert them the same way.

## Plugin tier audit (don't re-litigate without new evidence)

**Tier 1 — verified dead and deleted 2026-09-22** (previously claimed done, but the files were still present with zero references — `git grep` for each name found nothing): `html5shim` (`libraries/html5shim/`), `jstorage.min.js` (both `libraries/jquery/` and `layouts/v7/lib/jquery/` copies). `handsontable` was already genuinely gone.

**garand-sticky — removed 2026-09-22.** Turned out to be a pure dangling reference: `libraries/garand-sticky/jquery.sticky.js` didn't exist anywhere in the repo, and no app code called `.sticky()`. `Vtiger_Loader`'s script registration checks `file_exists()` before emitting a `<script>` tag, so this was already fully inert — just removed the stale registration line from `modules/Settings/LayoutEditor/views/Index.php`.

**colorpicker — replaced with native `<input type="color">` 2026-09-22.** Was actively used by Picklist (Sales Stage-style color swatches), Calendar (activity type / feed colors), and Project (task status colors) — all three now use a plain `<input type="color">` that doubles as the value-holding field (merged with the old separate hidden `[name=selectedColor]`/`.selectedColor` input in each case, one element instead of two). `app.helper.initializeColorPicker` and its Calendar.js/SharedCalendar.js duplicates are gone; each call site's `ColorPickerSetColor(...)` became a plain `.val(...)`. Along the way, fixed a real pre-existing bug in `Calendar.js`'s `registerAddActivityTypeEvent`: it read `.selectedUserColor` (a class that doesn't exist in any template) instead of `.selectedColor`, so that flow always saved with `selectedColor === undefined`. Verified live: Picklist (Sales Stage) and Calendar (both "Edit Calendar View" prefill and "Add Calendar View" random-color paths) round-trip correctly through save. Project's `EditColor.tpl`/`Detail.js` got the identical treatment but wasn't live-tested (no Project records existed to open the color editor on) — reviewed carefully and syntax-clean, matches the exact pattern proven twice elsewhere.
Removed the vendor library (`libraries/jquery/colorpicker/`) and its script/CSS registrations from all 3 PHP controllers.
**Known gap:** `pkg/vtiger/modules/Projects/Project/modules/Project/views/Detail.php` (the packaging mirror tree, out of scope per earlier decision) still references the old colorpicker.js — if that tree is ever used to regenerate/reinstall the Project module, it'll reintroduce the old dependency there.

**libraries/bootstrap-legacy — confirmed still load-bearing, keep 2026-09-22.** `modules/Migration/views/Index.php` is a real, admin-gated (`checkPermission` requires `isAdminUser()`) upgrade wizard used by `vtlib/Vtiger/PackageUpdate.php` for future core-version schema migrations — not dead code. It loads `bootstrap-legacy/css/bootstrap.min.css` only for its own isolated `vlayout` templates (`layouts/vlayout/modules/Migration/*.tpl`), which never mix with the BS5-migrated `v7` layout. Zero conflict with the BS5 migration; deleting it would break future core upgrades for no UI benefit. Decision: keep, don't touch.

**Tier 2 — deferred to advanced-customization phase** (deep functional coupling, not a safe "UI pass" item):
- `select2` — v3.4.8 in use (`select2.jquery.json` confirms), ~22 modules/44 call sites use `.select2("val"/"data"/"enable"/"disable"/"destroy", ...)` — all valid v3 API syntax, none of it breaks under v3, so no version bump needed and no JS changes required. **Bug found and actually fixed 2026-09-22** (the earlier "CSS-only visual refresh" entry in this log was itself wrong — verified live in-browser this time, not just claimed): `layouts/v7/lib/modern/css/editview.css` lines ~81-102 targeted select2 **v4**'s BEM class names (`.select2-selection--single`, `.select2-selection__rendered`, `.select2-container--focus`, `.select2-results__option--highlighted`), none of which exist in the loaded v3.4.8 library's DOM output — those rules never matched anything, confirmed via `getComputedStyle()` on a live Leads Edit page (still showing select2's stock `#ccc` border / 28px height / white bg before the fix). Replaced with real v3 class names (`.select2-choice`, `.select2-container-multi .select2-choices`, `.select2-container-active`/`.select2-dropdown-open`, `.select2-drop`, `.select2-results .select2-highlighted`) using the same design tokens. Re-verified live after the fix: closed state shows `--line` border/`--mist` bg/36px height/`--ink` text, open state shows white bg + `--teal` border, and the highlighted dropdown row shows `--teal` bg/white text — all confirmed via computed styles and a screenshot on Leads Edit. This CSS file is served as a static asset (not Smarty-compiled), so no `templates_c` cache clear is needed for changes to it, just a browser refresh. Also flagged, not yet resolved: `select2-bootstrap.css` exists in two places (`layouts/v7/lib/jquery/select2/` and `layouts/v7/lib/select2-bootstrap/`, both loaded via `Header.tpl`) with BS3-era `.form-control` cascade rules — worth checking whether either is now fully dead weight, but not verified yet.
- `chosen` — **resolved 2026-09-22, moved out of Tier 2: fully dead in v7 scope, no migration needed.** The plugin (`libraries/jquery/chosen/`), its `_chzn` wrapper abstraction (`initializeChosen`/`destroyChosen`/`getChosenElementFromRaw`, `useSuffix: "_chzn"`), and every real call site (`Workflows/Edit1.js`, `Emails.js`, `Header.js`, `Edit.js`, `CustomView.js`, `Reports/Edit1.js`) live entirely inside `layouts/vlayout/` — the legacy skin tree already out of scope for this migration — including the wrapper's home file, root `resources/app.js`, which is `<script>`-loaded only by `layouts/vlayout/modules/Vtiger/JSResources.tpl`, never by v7's Header.tpl/JSResources.tpl. The one v7-scope trace, `layouts/v7/modules/Vtiger/resources/dashboards/Widget.js`'s `initSelect2Elements` function, was dead code twice over (its only caller was commented out, and it called `.chosen()` on a plugin never loaded in v7 anyway) — removed that function and its commented-out call site.
- `bootstrap-datepicker` — **resolved 2026-09-22, replaced with flatpickr v4.6.13 (vendored locally at `layouts/v7/lib/flatpickr/`).** This was a much bigger bug than "dead CSS": the earlier log entry undersold it. Root cause — `modules/Vtiger/views/Popup.php`/`Basic.php` registered `libraries/bootstrap/js/eternicode-bootstrap-datepicker/js/bootstrap-datepicker`, a path that **doesn't exist on disk at all** (confirmed via `ls`), so `Vtiger_Loader`'s `file_exists()` check silently dropped it — the real bootstrap-datepicker JS never loaded, app-wide. Every `.datepicker(...)` call site was actually hitting **jQuery UI's** Datepicker instead (confirmed live via `jQuery.fn.datepicker.toString()` showing `$.datepicker.initialized`/`_checkExternalClick`, jQuery UI Datepicker internals), which silently ignores bootstrap-datepicker-only options (`autoclose`, `todayBtn`, `format`, `todayHighlight`, `clearBtn`) and never fires `changeDate`. **Confirmed live impact before the fix:** picking a date on Opportunities' Close Date field saved as `09/15/2026` — jQuery UI's hardcoded default `mm/dd/yy` — completely ignoring the user's configured `date_format` preference (`mm-dd-yyyy` in this case). This is a real data-entry bug, not cosmetic.
  Only 3 real call sites existed app-wide: the central `registerEventForDateFields` in `layouts/v7/modules/Vtiger/resources/Utils.js` (called from 9 modules — the actual blast radius), `Calendar.js`'s goto-date button, and `MailManager/List.js`'s teardown call. Only 3 date-format values exist anywhere in the app (`dd-mm-yyyy`, `mm-dd-yyyy`, `yyyy-mm-dd` — confirmed by grepping every `date_format` `<option>` in the codebase), so the format-token translation to flatpickr's convention is a tiny fixed map (`d-m-Y`/`m-d-Y`/`Y-m-d`).
  Rewrote all 3 call sites to use `flatpickr()` directly, added a small custom Today/Clear footer (flatpickr has no built-in equivalent) via `onReady`, registered `flatpickr.min.js`/`.min.css` in `Vtiger/JSResources.tpl`/`Header.tpl` right after jquery-ui, and themed `.flatpickr-*` classes with the design tokens in `editview.css`. Dropped the never-functional `language` option (no locale files were ever vendored, so it did nothing before either). jQuery UI itself is untouched/still loaded (needed for sortable/draggable/resizable/dialog/autocomplete, 28+ files) — only its Datepicker widget is now unused within that bundle, which is harmless since it's one bundled file.
  **Verified live:** Opportunities Close Date now renders flatpickr with teal theming, working Today/Clear buttons, and saves as `09-16-2026` (correct `mm-dd-yyyy` format) instead of the old `09/15/2026`. Calendar's goto-date button also confirmed initialized with flatpickr (`_flatpickr` present) with the correct format.
- `bootstrapSwitch` — **resolved 2026-09-22, replaced with native Bootstrap 5 `.form-check.form-switch` (no new vendor library — BS5 was already loaded).** 7 real usages found (beyond the 4 JS files originally noted, 7 templates also had markup): `fieldPresence` (FieldCreate.tpl), `collapseBlock` (FieldsList.tpl, both the live row and the `#hiddenCollapseBlock` clone template), `rollupcomments` (RecentComments.tpl, ShowAllComments.tpl), `workflowstatus` (ListViewRecordActions.tpl), `taskStatus`/`tmpTaskStatus` (TasksList.tpl), and `duplicateCheck` (DuplicateHandling.tpl). All were plain `<input type="checkbox" class="bootstrap-switch">` with `style="opacity:0"` (bootstrapSwitch hides the real input and draws its own markup on top) — converted each to `<div class="form-check form-switch"><input class="form-check-input" role="switch" ...></div>`, dropping the opacity hack since the native input is now the visible control.
  JS conversions across `Workflows/List.js`, `Settings/LayoutEditor/LayoutEditor.js`, `Workflows/Edit.js`, `Vtiger/Detail.js`: `.bootstrapSwitch()` init calls removed outright (no init needed for a native checkbox); `.bootstrapSwitch('state', bool, true)` → `.prop('checked', bool)` (native `.prop()` never fires `change`, so the `skipOnChange` semantics come for free); `.bootstrapSwitch('toggleReadonly', bool)` → `.prop('disabled', bool)` (checkboxes don't honor the HTML `readonly` attribute — `disabled` is the correct native mechanism); `.bootstrapSwitch('handleWidth'/'labelWidth'/'size', ...)` calls dropped (native switch sizing is CSS-only, see below); `switchChange.bootstrapSwitch` event → plain `change`, deriving state via `jQuery(e.currentTarget).is(':checked')` instead of the plugin's `(e, state)` callback arg.
  **Bug caught during conversion:** `Workflows/List.js`'s `registerRowClickEvent` had a guard `targetElem.closest('.bootstrap-switch')` to stop clicks on the status switch from also triggering row-navigation — removing the `bootstrap-switch` class broke this guard, so clicking the switch briefly also navigated into the record. Fixed by updating the guard to `.closest('.form-switch')` (the new wrapper div's class). Worth checking for the same pattern if any other Tier 2 plugin removal touches a listview row with an inline interactive control.
  Theming: added `.form-switch input.form-check-input[type="checkbox"]` rules to `editview.css` (loaded globally via Header.tpl, applies everywhere) sized at 2.75em×1.5em with `--mist`/`--line` off-state and `--teal` on-state, per the user's request for a modern rounded/pill-style switch. Needed `!important` — `listview.css`'s `.listview-table input[type="checkbox"]` rule (higher specificity than a plain 2-class selector) was forcing switches inside list rows back down to 15×15px; discovered via runtime `document.styleSheets` inspection, not guesswork. Verified live: Workflows list status switches, LayoutEditor's "Collapse Block" switch, all render as teal rounded pills and toggle correctly (confirmed via dispatched mouse events at the exact element center — manual screenshot-coordinate clicking in the browser tool was unreliable due to a viewport/screenshot scale mismatch, not a product bug).
- **`gantt` and `gridster` — scoped 2026-09-22, deferred to Phase 2 (advanced customization), not swapped.** Unlike every other Tier 2 item resolved this pass, these two are not funneled through a thin internal interface that a rewrite can hide behind — they're deeply, directly integrated into real application logic:
  - `gantt` (`libraries/jquery/gantt`) — `Project/resources/Detail.js` instantiates `GanttMaster` directly, with custom drag/drop task-sorting, an undo/checkpoint stack, and event bindings (`updateTaskRecord.gantt`, `zoomPlus.gantt`, `resize.gantt`) wired straight to ProjectTask CRUD. This *is* the Project Gantt-chart feature, not a decorative widget around it.
  - `gridster` (`libraries/jquery/gridster`) — `Vtiger/resources/DashBoard.js` calls `add_widget`/`remove_widget` directly and persists layout via `savePositions()` across multiple dashboard tabs. This *is* the dashboard's drag-and-rearrange-widgets engine.
  Both are functioning correctly today — no security hole or silent failure like jqplot's `$.browser` crash or bootstrap-datepicker's wrong-format bug — they're just old. Replacing either means re-implementing real drag/resize/persistence/undo application logic against a new library's API, not swapping a rendering call behind an existing wrapper. User decision: defer both, consistent with the existing `pjax` and select2-v4 deferrals for the same "deep functional coupling" reason.

**ckeditor — resolved 2026-09-22, upgraded 4.3.1 (2013) → 4.22.1 (vendored via the `ckeditor4` npm package, replacing the whole `libraries/jquery/ckeditor/` tree).** Real usage is funneled through one wrapper, `layouts/v7/modules/Vtiger/resources/CkEditor.js`'s `Vtiger_CkEditor_Js` class, using only `CKEDITOR.replace`/`.remove`/`.instances`/`.setData`/`.document.getBody().getText()`/`.updateElement()`/`.destroy()` — all present with identical signatures across the entire CKEditor 4.x line, so **zero JS call-site changes** were needed, only the vendor files.
  **Version ceiling matters here:** CKEditor 4.23.0+ requires a paid "Extended Support Model" commercial license and throws `[CKEDITOR]: The license key is missing or invalid.` at runtime without one — confirmed live by first trying 4.25.2 (npm's `latest` tag) and hitting that exact error in-browser. **4.22.1 is the last free/open-source 4.x release** and is what's actually installed. Don't bump past it without buying a license.
  `config.js` is NOT vendor code — it's vtiger's own toolbar/plugin customization (`removePlugins`, `fullPage`, `allowedContent`, `enterMode`/`shiftEnterMode`, the kcfinder `filebrowserBrowseUrl`/`filebrowserUploadUrl` integration — confirmed `kcfinder/browse.php` still exists in-repo, custom `toolbarGroups`, extra cursive `font_names`) and was preserved verbatim across the swap, since `cp -R`-ing the fresh npm package over the old directory overwrites it with CKEditor's own default config. One thing intentionally dropped from the old config: an explicit `config.plugins` whitelist hardcoded against 2013's plugin taxonomy — most of those names (e.g. `basicstyles`, `toolbar`, `wysiwygarea`, `undo`) were later merged into CKEditor 4's core bundle and are no longer separate loadable plugins, so re-asserting that stale list against 4.22.1 risked breaking initialization. Removed `samples/`, `bender-runner.config.json`, and packaging metadata (`package.json`/`bower.json`/`composer.json`/`CHANGES.md`/`README.md`/`SECURITY.md`) — none are runtime-loaded, matching how other vendored libraries in this repo are trimmed.
  Verified live in a clean browser tab (a stale tab falsely showed the 4.25.2 license error after the downgrade — a real gotcha, always test cache-sensitive vendor swaps in a fresh tab, not a reused one): `CKEDITOR.version` reports `4.22.1 (Standard)`, the custom toolbar groups render correctly, `setData()`/`getData()` round-trip through the `fullPage`-wrapped HTML document correctly, `.document.getBody().getText()` (the wrapper's `getPlainText()`) returns correct plain text, and the Link dialog opens with all 4 tabs including the kcfinder "Browse Server" button intact.

**jqplot — resolved 2026-09-22, replaced with Chart.js v4.5.1 (vendored at `layouts/v7/lib/chartjs/chart.umd.js`).** All real usage funneled through exactly one internal interface layer: `libraries/jquery/vtchart.js`'s `$.fn.vtchart(data, options)` jQuery plugin (itself delegating to `libraries/jquery/VtJqplotInterface.js`'s `vtJqPlotInterface` class) — no app code ever called `.jqplot()` directly. This meant the whole migration was containable to **one file**: rewrote `vtchart.js` in place to build Chart.js charts instead of delegating to jqplot, keeping the exact same public signature (`element.vtchart(data, options)` with `options.renderer`), so its 2 real callers (`Vtiger/resources/dashboards/Widget.js` — pie/bar/multibar/funnel dashboard widgets, `Reports/resources/ChartDetail.js` — horizontalbar/linechart report charts) needed zero changes. `VtJqplotInterface.js` and the whole `libraries/jquery/jqplot/` vendor tree were deleted.
  Renderer mapping confirmed against actual data shapes read from the 2 caller files (not guessed): `pie` (array of `[label, value, id]` triples → Chart.js pie), `bar` (single-series `[[values]]` + category labels → Chart.js bar), `multibar` (per-series value arrays + ticks/labels → Chart.js stacked bar, dataset index = jqplot's old `gridpos`), `horizontalbar` (per-series `[value, position]` pairs → Chart.js bar with `indexAxis:'y'`), `linechart` (per-series value arrays → Chart.js line, multi-dataset). `column` was dead code in the original interface — grepped confirmed zero callers anywhere in the app, not carried over.
  `funnel` has no native Chart.js equivalent — approximated with a horizontal bar chart in the same top-to-bottom stage order as the source data (reads the same way a funnel does, widest/first stage on top). Documented as an intentional approximation, not a gap.
  Click-through and hover-cursor behavior preserved: old `jqplotDataClick`/`jqplotDataMouseOver` events replaced with Chart.js's `onClick`/`onHover` config callbacks, both still triggering the same `vtchartClick` custom event via `.triggerClick()` that caller code already listens for — the link-lookup logic per renderer (`links[index]`, `links[gridpos][index]` for multibar, `links[index].links` for funnel) was ported unchanged from the old `registerClick` switch statement.
  Removed 2 now-meaningless `jQuery('table.jqplot-table-legend').css(...)` cleanup lines in `ChartDetail.js` — Chart.js draws its own canvas legend, that DOM selector can never match again.
  Colors resolved from the live CSS custom properties (`--teal`, `--teal-deep`, `--danger`, `--muted`) at chart-build time via `getComputedStyle`, so chart theming automatically tracks the design tokens rather than being hardcoded.
  Verified with synthetic data matching each real caller's exact shape (not just smoke-tested): all 6 renderer types render correct canvases with proportionally correct values (screenshot-confirmed), the pie chart's click handler correctly resolves `links[1]` on a simulated click at index 1, and the linechart's two series both carry correct, independent data (confirmed via `Chart.getChart(canvas).data.datasets`). No `$.browser` dependency remains from jqplot — the shim in `Header.tpl` stays regardless, since `Reports/PrintReport.tpl` has a real, unrelated `jQuery.browser.webkit` usage.

**Tier 3 — kept as-is** (still fine): `jquery-validation`, `daterangepicker` (dangrossman), `perfect-scrollbar`, `pdfjs`, `Viewer.js`, `video-js`.

**pjax — explicitly not touched.** 21 real `.pjax()` calls in core navigation (`List.js`, `Detail.js`, `application.js`). This is AJAX-navigation infrastructure, not a UI/plugin concern — replacing it is an architecture change for the advanced-customization phase.

## Screens already rebuilt/skinned

- **Login page** (`Login.tpl`) — full rewrite, self-contained (no shared JS deps), dropped bxSlider/mCustomScrollbar for vanilla JS/CSS. Smarty wiring preserved exactly: `$ERROR`, `$MESSAGE`, `$CUSTOM_SKINS`, `$JSON_DATA`, forgotPassword flow.
- **List/Detail/Nav/Edit views** — CSS-only skin layers (`listview.css`, `detailview.css`, `nav.css`, `editview.css`), zero markup changes. These views are deeply wired to JS via exact classnames (`.listview-table`, `.fieldBlockContainer`, sort/inline-edit/quick-preview handlers) — a full rewrite here would break core CRM functions. Only the login page was safe to fully rewrite because it's isolated.

## EchoCrew brand tokens + UI stability pass (2026-09-22)

**Design tokens were wrong — corrected by pulling real computed values from `dev.echocrew.in`.** The original tokens set at project start (`--teal:#0E7C66`, `--ink:#10151C`, Space Grotesk headings) were invented, not matched from the actual brand site. Fetched the live site's `:root` custom properties via `getComputedStyle` and remapped our token *values* to match exactly, keeping our variable *names* so no call site needed to change:
```
--ink:#0B0F14        (was #10151C)
--teal:#00D3B8       (was #0E7C66 — much brighter/more saturated real brand accent)
--teal-deep:#06A793  (was #0A5C4B)
--teal-soft:#E4FBF6  (was #E8F7F1)
--mist:#F6F7F5       (was #F4F6F5)
--mist-dark:#EDEFEC  (was #EDF0EF)
--muted:#4A555F      (was #6B7280)
--line:#DFE2DD       (was #E4E8E6)
```
Added `--radius-md:14px`, `--radius-lg:22px`, `--shadow`, `--shadow-sm` (all from the real site's tokens) for the "buttons shouldn't look flat" ask. Heading font corrected Space Grotesk → **Sora** (7 usage sites + the Google Fonts `<link>` in `Header.tpl`) — Sora is what the real site actually uses, Space Grotesk was never real.
`:root` lives in `listview.css` (loaded globally) — but `login.css` loads in isolation (confirmed via grep: `Login.tpl` includes only `login.css`, nothing else), so it had the **old** palette **hardcoded as literal hex**, not `var()`. Gave it its own duplicate `:root` block with the same corrected values instead of leaving it stale. Also caught 2 places using hardcoded `rgba(14,124,102,...)` (old teal's literal RGB) for focus-ring shadows in `editview.css` — `var()` substitution doesn't reach into a raw `rgba()` triplet, had to fix those by hand. **Lesson: after changing a token's hex value, grep for that hex/rgb literal across the whole modern CSS tree — don't assume everything uses `var()` just because the token system exists.**

**Found and fixed a second wave of the exact same bug class as `.close`/`.caret`/`.btn-default` from the original migration: Bootstrap 3 class names BS5 never defines, silently rendering unstyled.** Same root cause every time — a class got renamed or restructured between BS3 and BS5, nobody did a full-tree grep, so every un-migrated instance silently lost all styling. Found this time:
- **`.btn-default`** (220 templates!) — confirmed zero rules for it anywhere in the BS5 bundle; only two hand-scoped containers (`editViewActions`, `detailViewHeader`) had ad-hoc overrides, leaving ~200 buttons app-wide with no background/border/color at all. This is what "buttons not stable all over the place" actually was.
- **`.btn-small`/`.btn-large`/`.btn-mini`/`.btn-xs`/`.btn-text`/`.btn-social`/`.btn-google`** — all Bootstrap 2/3 sizing/variant classes with zero BS5 definition, same fate.
- **`.input-group-addon`** (31 templates) — BS4/5 renamed this to `.input-group-text` with a completely different flex layout model. Confirmed live via `getBoundingClientRect()`: the search/create icon buttons next to every reference (lookup) field rendered with a visible **15px floating gap** from the input instead of a flush, merged addon (`display:block`, no background, no border — just a bare icon floating near the input). This was the "combo inputs breaking" / "add-on buttons UI" / uitype-10-field complaints — reference fields are exactly this markup pattern.
Fix pattern for all of the above, consistent with the original `.close` precedent: **restyle the dead class names globally in `nav.css`** (guaranteed loaded everywhere) rather than rewrite hundreds of template call sites. `.input-group`/`.input-group-addon` got a real flex-based reimplementation of BS4/5's input-group model (border-merging, flush corners, no-shrink icon column) scoped to the old class names.

**Modal close button was on the wrong side — root-caused via `getBoundingClientRect`, not guesswork.** `ModalHeader.tpl` floats the close button (`float-end`) and title (`float-start`) inside one `<div class="clearfix">`. BS5's `.modal-header` is `display:flex`, and a plain block child with no explicit width inside a flex row **shrinks to fit its content** instead of spanning the full row — confirmed live: the `.clearfix` div measured ~202px inside a 498px-wide header. The floated close button correctly reached the *edge of that 202px box*, which put it immediately next to the title instead of the modal's true right edge. Fixed with one rule, `.modal-header .clearfix{flex:1 1 auto;width:100%}`, in `nav.css` — global, every modal in the app uses this same template.

**Detail-view action buttons could run off-screen on modules with more top-level actions than Leads has.** `DetailViewActions.tpl` hardcodes `col-lg-6` on the button toolbar container regardless of how many `DETAILVIEW_LINKS['DETAILVIEWBASIC']` buttons a given module renders before the "More" dropdown — some modules have far more than others. At a fixed 50% column width with no wrap, extra buttons overflowed past the visible edge. Fixed in `detailview.css`: `.detailViewButtoncontainer{width:auto;flex:none}` plus `flex-wrap:wrap` on the toolbar/btn-group — verified at a 480px mobile viewport that 5 buttons now wrap onto 2 rows instead of overflowing. Also gave `.detailview-header-block` a proper card treatment (background/border/radius) since it previously had none.

**Detail view's "Key Fields" sidebar widget (`.summary-table`) had zero label/value column styling** — a completely separate class from the main `.detailview-table` that already had rules. `table-layout:fixed` auto-sized the label column down to ~49px with no explicit width, so labels and values rendered visually touching (`LAST NAMETestLead`, no gap). Added matching `.summary-table td.fieldLabel`/`td.fieldValue` rules mirroring the main table's treatment.

**Radio buttons and the jquery.timepicker dropdown were both plain unstyled vendor/browser defaults** — never touched since this migration began. Radios fixed with `accent-color:var(--teal)` (same technique already used for checkboxes). Timepicker dropdown themed to match flatpickr's card style (border/radius/shadow) with teal selected-state, since the two sit on the same forms and looked completely inconsistent with each other.

## Debugging approach that works here

1. Pull the latest repo state before diagnosing (`git pull`; watch for local uncommitted test edits blocking the pull).
2. When something "still looks broken after the fix," check in this order: (a) was the fix actually applied/pushed, (b) is Smarty cache stale, (c) is browser cache stale (`?v=8.4.0` doesn't bust Smarty server-side cache).
3. For layout bugs, get real computed-style data via browser DevTools console (`getComputedStyle`, `getBoundingClientRect`) rather than guessing from screenshots repeatedly — this found the exact `flex-wrap:nowrap` 50/50-split bug fast once real numbers were in hand.
4. Read the actual rendered HTML output when available (view-source or a pasted DOM dump) — this is far more reliable than reasoning about what the templates "should" produce, especially once several rounds of edits have happened.
5. **CSS comment gotcha discovered 2026-09-22, twice in one session: never write a file-glob path like `skins/*/style.css` inside a `/* ... */` comment.** The substring `*/` embedded in `/*/` closes the comment early, silently turning the rest of the intended comment text into garbage CSS tokens that swallow the real rule immediately after it — the rule doesn't error, it just vanishes from the parsed stylesheet (`document.styleSheets` won't show it at all). This happened twice in the same editing session (once in `listview.css`'s `body` rule, once in `nav.css`'s navbar `z-index` rule) and both times manifested as "I wrote the !important fix but the browser still shows the old value" — which looks identical to a browser-cache problem, wasting time chasing the wrong cause. Always verify a suspicious missing rule by checking `document.styleSheets[i].cssRules` directly (not just `getComputedStyle`) when a fix "isn't applying" despite `!important` and a confirmed-correct file on disk. Write "skins subfolder's style.css" or similar in prose instead of the literal glob.

## Per-app skin files silently override the modern layer (2026-09-22)

Discovered while chasing several "already fixed but still broken" reports: `layouts/v7/skins/{marketing,sales,inventory,contact,project,support,tools,marketing_and_sales}/style.css` (one loaded per active "app"/module-group context, all near-identical copies of the original BS3-era skin) load **after** `nav.css`/`listview.css`/`editview.css`/`detailview.css` and still carry a large amount of original BS3 styling for shared class names our modern layer also targets. Where specificity is equal (same selector, e.g. bare `body` or single-class `.close`), the skin's later-loaded rule silently wins — a fix can be 100% correct in the modern CSS and still not appear live, with no error anywhere, because a different property value from the skin cascades in in its place.

Confirmed real conflicts found this session (all fixed with `!important` in the modern-layer file rather than editing 8 skin files):
- `body{font-family:OpenSans-Regular,sans-serif;font-size:12px}` — overrode the Roboto/Sora font-family choice on **every page**, app-wide. This was the actual root cause of "fonts inconsistent everywhere."
- `.btn-success{font-size:12px;padding:2px 6px}` — cramped every success/primary button on marketing-skinned pages.
- `.cancelLink, .popoverCancel{padding-left:10px;color:red}` — partially fought the new Cancel-button styling.
- `.close{color:#c3c3c3 / #fff / #000}` (three different rules depending on context) — close-icon color varied by which skin/page you were on.
- `.search-link{border:thin solid #DDD;float:left;margin:4px}` — gave the dark navbar's search box a stray light-gray border and BS3 float layout.
- `.quickCreateContent .input-group{display:table}` — squeezed Quick Create's date/time inputs down to ~22px wide (higher specificity than a bare `.input-group`, so this one wasn't even an equal-specificity fight — it won outright).
- `.module-nav{z-index:1093}` (the left sidebar — both the Settings menu and the module-icon rail share this class) — higher than BS5's own `.fixed-top` navbar (`z-index:1030`), so the sidebar rendered on top of the navbar's logo/hamburger corner once a page scrolled far enough. Fixed by raising the navbar to `z-index:1094` in `nav.css` — deliberately *not* higher than that, since `#helpPageOverlay` (the right-sliding Quick Preview panel) sits at `z-index:1099` by design, on top of the navbar, so its own header/close button aren't buried when open. Order that matters: sidebar `1093` < navbar `1094` < quick-preview overlay `1099`.
- `#helpPageOverlay{width:300px;right:0px}` (correct, right-anchored by design) vs BS5's `.modal{left:0px}` (different property, so not a specificity conflict — **both applied simultaneously**). With an inline `width:550px` also set, having both `left:0` and `right:0` pinned over-constrains the box, and per the CSS positioning spec the browser drops `right` and anchors via `left` — this is why the Quick Preview slide-in panel opened from the **left** instead of the right. Fixed with `#helpPageOverlay.modal{left:auto !important}`.

**Lesson for future work on this codebase:** when a fix "doesn't seem to apply" and the file on disk is confirmed correct, check `document.styleSheets` for the *actual* winning rule before assuming a cache issue — there's a good chance one of the 8 skin files is the real culprit, not staleness. This class of bug is probably not fully exhausted; if another shared classname turns up unstyled or inconsistently styled despite a rule existing in the modern layer, check the skin files before anything else.

## Other structural fixes from the 2026-09-22 UI-stability pass

- **`.btn-default` used across ~220 templates had zero styling** (Bootstrap 3 class, BS5 never defines it — same class of bug as `.close`/`.caret` from the original migration). Same for `.btn-small`/`.btn-large`/`.btn-mini`/`.btn-xs`/`.btn-text`/`.btn-social`/`.btn-google`. Fixed globally in `nav.css`.
- **`.input-group-addon` (31 templates)** — BS4/5 renamed it `.input-group-text` with a different flex layout model; unstyled, the search/create icon buttons next to reference (lookup) fields floated with a visible gap instead of a flush addon. Rebuilt the BS4/5 input-group model against the old class name.
- **Modal close button rendered next to the title instead of the modal's right edge.** `ModalHeader.tpl` floats title/close inside one `<div class="clearfix">`; BS5's `.modal-header` is `display:flex`, which sizes a plain block child to its *content* width instead of the full row — confirmed live, the clearfix measured ~40% of the real header width. Fixed with `.modal-header .clearfix{flex:1 1 auto;width:100%}` — one rule, every modal in the app.
- **`AddDashBoardTabForm.tpl` used `.col-lg-5`/`.col-lg-6` grid classes with no `.row` wrapper** — without it, BS5's flex grid never activates and the columns stack as plain blocks instead of sitting side by side, producing a large dead gap between the "Dashboard Name" label and its input. Added the missing `.row`. Worth checking other legacy modal templates for the same missing-`.row` pattern if similar "fields look broken/huge gap" reports come in.
- **`.summary-table` (the Detail View "Key Fields" sidebar widget) had zero label/value column-width rules** — a separate class from `.detailview-table`, which already had them. `table-layout:fixed` auto-sized the label column down to ~49px, so labels and values rendered touching with no gap (`LAST NAMETestLead`). Added matching rules mirroring `.detailview-table`'s.
- **`DetailViewActions.tpl` hardcodes `col-lg-6` on the action-button toolbar** regardless of how many top-level buttons a given module renders before "More" — modules with more actions than Leads has could overflow past the visible edge. Fixed with `width:auto;flex:none` + `flex-wrap:wrap` — verified at a 480px viewport that buttons wrap onto multiple rows instead of running off-screen.
- **Checkboxes for boolean (uitype 56) fields converted to CSS-only toggle switches** (`appearance:none` + a `::before` pseudo-element knob) to match the bootstrapSwitch replacements done earlier in the migration — these have no wrapping markup to hang a real `.form-switch` div on, so the slider look is built directly on the `<input>`. Needed `!important` since `Boolean.tpl` sets an inline `style="width:15px;height:15px"` on the checkbox, and inline styles beat any class selector except `!important`.
- **Font swapped from Sora/Inter to Roboto app-wide**, per explicit user instruction overriding the earlier EchoCrew-brand-matched choice (Sora/Inter had been correctly matched from `dev.echocrew.in`'s real computed styles two sessions ago — this is a deliberate user override of that decision, not a correction of a mistake). Updated the Google Fonts `<link>` in `Header.tpl` and every `font-family` declaration across `nav.css`/`listview.css`/`editview.css`/`detailview.css`/`login.css`.
- **Known remaining issue, not yet fixed:** the Quick Preview slide-in panel (`#helpPageOverlay`) no longer closes on an outside click, despite the position/close-button fixes above. This is a JS event-handling regression (delegated click-outside handler not firing or not bound), not a CSS issue — needs its own investigation into whichever script wires up `helpPageOverlay`'s outside-click dismissal (likely `List.js` or `Vtiger.js`).
- **Known remaining issue, not yet fixed:** the list view's inline search-toggle row (`<th class="inline-search-btn">`, toggled by the "Search" button in the column header) renders as an oversized `<th>` with other cells in the same `<tr>` collapsing to zero width, and inspecting its content showed unrelated leaked text ("No matches found...vtiger admin...") bleeding into one of the cells — this looks like a deeper template/table-structure issue, not a simple CSS layout fix, and needs dedicated investigation before touching it.
